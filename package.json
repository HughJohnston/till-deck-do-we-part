{
  "name": "till-deck-do-we-part",
  "version": "1.0.0",
  "description": "Till Deck Do We Part - A wedding gift endless runner game",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "generate-placeholders": "npx tsx scripts/generate-placeholders.ts",
    "deploy": "npm run build && cp scripts/gh-pages-vercel.json dist/vercel.json && gh-pages -d dist",
    "deploy:vercel": "npm run build && git push origin main"
  },
  "license": "ISC",
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "phaser": "^4.1.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.8.17",
    "gh-pages": "^6.3.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
